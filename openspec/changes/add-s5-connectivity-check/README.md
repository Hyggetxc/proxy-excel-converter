# add-s5-connectivity-check

为 S5 URL 解析结果增加一键连通性检测能力。

## 目标

- 解析完成后支持一键检测代理是否能访问海外轻量探针站点。
- 每条解析结果展示三列独立检测结果：Google、Cloudflare、Apple。
- 不做汇总列，用户自行根据三列结果判断代理质量。

## 边界

- 浏览器静态页面不能直接通过用户输入的 SOCKS5 代理发请求，因此检测必须依赖本地或线上 API。
- 复制功能仍只复制导出配置，不复制检测状态。
